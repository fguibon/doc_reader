import 'package:flutter/material.dart';

class InformationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
        body: Container(
            child: Column(children: <Widget>[
      Text(
          "Sub menu [buttons/bars etc] showing about 20 medical conditions – if menu selected then move to DETAIL CONDITION PAGE [see section below]."),
    ])));
  }
}
