import 'package:flutter/material.dart';

class EmergencyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: Text("Sub menu [buttons/bars etc] showing about 20 medical conditions – if menu selected then move to EMERGENCY ACTION PAGE [see section below].")
    );
  }
}